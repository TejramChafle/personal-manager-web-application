import { IsautofocusedDirective } from './isautofocused.directive';

describe('IsautofocusedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsautofocusedDirective();
    expect(directive).toBeTruthy();
  });
});
